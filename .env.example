# Pauly Configuration Reference
# Copy this file to ~/.config/pauly/config and update values
#
# All values are loaded as bash variables. Use double quotes for strings.
# Run 'pauly config' for an interactive setup wizard.

# ==============================================================================
# CORE SETTINGS
# ==============================================================================

# Email address to receive notifications and alerts
EMAIL=""

# Directory containing your git repositories (absolute path)
# Default: ~/Projects
PROJECTS_DIR="$HOME/Projects"

# ==============================================================================
# SMTP CONFIGURATION (for sending emails)
# ==============================================================================

# SMTP server settings (Gmail defaults shown)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""

# Gmail App Password (generate at https://myaccount.google.com/apppasswords)
# Format: xxxx xxxx xxxx xxxx (16 chars with spaces)
SMTP_PASSWORD=""

# ==============================================================================
# IMAP CONFIGURATION (for receiving email tasks)
# ==============================================================================

# IMAP server settings (Gmail defaults shown)
IMAP_HOST="imap.gmail.com"
IMAP_PORT=993

# Comma-separated list of email addresses allowed to send tasks
# Only emails from these senders will be processed
ALLOWED_SENDERS=""

# ==============================================================================
# GITHUB TASKS CONFIGURATION
# ==============================================================================

# GitHub repository to watch for task issues (format: owner/repo)
# Create issues with the configured label to trigger Pauly tasks
GITHUB_TASKS_REPO=""

# Label to filter issues (default: "pauly")
# Only issues with this label will be processed
GITHUB_TASKS_LABEL="pauly"

# ==============================================================================
# DEV MODE CONFIGURATION
# ==============================================================================

# Default project directory for dev mode operations (absolute path)
# Used when triggered via email or GitHub Issues without explicit project
DEV_PROJECT_DIR=""

# ==============================================================================
# ADMIN DASHBOARD CONFIGURATION
# ==============================================================================

# Port for the admin web dashboard (default: 3001)
ADMIN_PORT=3001

# Restrict dashboard access to specific IP addresses
# Leave empty to allow all IPs (localhost always allowed)
# Example: "192.168.1.0/24" or "10.0.0.5"
ADMIN_ALLOWED_IP=""

# ==============================================================================
# AUTO-FIX CONFIGURATION
# ==============================================================================

# Enable auto-fix to automatically create PRs when jobs fail due to code bugs
# Values: "true" or "false"
AUTOFIX_ENABLED="false"

# Which scripts can trigger auto-fix
# "all" = all scripts, or comma-separated list of script filenames
# Example: "daily-claude-summary.sh,git-health-check.sh"
AUTOFIX_SCRIPTS="all"

# Maximum fix attempts per failure
AUTOFIX_MAX_ATTEMPTS=1

# Git branch prefix for auto-fix branches
# Branches will be named: <prefix>/<script>-<timestamp>
AUTOFIX_BRANCH_PREFIX="autofix"

# ==============================================================================
# LOGGING CONFIGURATION
# ==============================================================================

# Maximum log file size in MB before rotation
MAX_LOG_SIZE_MB=10

# Number of rotated log files to keep
MAX_LOG_FILES=5

# ==============================================================================
# MONITORING CONFIGURATION
# ==============================================================================

# Healthchecks.io ping URL for uptime monitoring (optional)
# Pauly will ping this URL after successful job runs
HEALTHCHECK_URL=""

# ==============================================================================
# NOTIFICATION WEBHOOKS (optional)
# ==============================================================================

# Slack incoming webhook URL for notifications
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=""

# Discord webhook URL for notifications
# Get from: Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=""

# ==============================================================================
# QUEUE CONFIGURATION (optional)
# ==============================================================================

# Enable SQLite-based task queue (replaces file-based locks)
# Values: "true" or "false"
QUEUE_ENABLED="false"
